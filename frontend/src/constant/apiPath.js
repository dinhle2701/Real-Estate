const BASE_URL = "http://localhost:5050"

const API_PATHS = {
    //property: `${BASE_URL}/api`,
    auth: `${BASE_URL}/api`,
    // cart: `${BASE_URL}/api`,
    // stat: `${BASE_URL}/api`,
    // order: `${BASE_URL}/api`,
    img: BASE_URL
};

export default API_PATHS;